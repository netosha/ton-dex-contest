import React from 'react';

import { NextPage } from 'next';

import Layout from '@components/Layout';

const Pool: NextPage = () => {
  return <Layout>Here will be pool page</Layout>;
};

export default Pool;
